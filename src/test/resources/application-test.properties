# Test Configuration - Disable SSL for tests
# Tests use MockMvc which doesn't require actual SSL certificates
server.port=0
# Override SSL properties to disable SSL for tests
server.ssl.key-store=
server.ssl.key-store-password=
server.ssl.key-store-type=
server.ssl.key-alias=

# Use PostgreSQL database for tests (same as production)
spring.datasource.url=jdbc:postgresql://localhost:5432/slaughter3?currentSchema=slaughterhouse
spring.datasource.username=postgres
spring.datasource.password=felipe123
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

